import express from 'express';
//import postRoute from './post.route.js'; // Import the post route from './post.route';

const router = express.Router();


/*app.listen(PORT, () => {
    console.log(`Server running on port ${PORT}`);      
});*/

export default router;